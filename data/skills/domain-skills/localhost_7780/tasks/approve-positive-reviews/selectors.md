# Selectors — localhost

所有关键元素（Marketing / All Reviews 菜单、reviewGrid_filter_status、Search、Reset Filter、行内 Edit 链接、status_id、save_button、back）均已在 sop_md 中以 id/aria-label/可见文本就地描述，唯一且可直接在 DOM 快照中定位，无需额外指纹表。