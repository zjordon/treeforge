# 接入层（server）—— P1 引入。
# 本期占位。届时在此实现 FastAPI 单文件 server.py，提供：
#   POST /v1/traces/init         分块上传初始化（upload_id = sha256(trace_id)[:12]，幂等）
#   PUT  /v1/traces/{id}/chunks/{index}  分块上传（gzip NDJSON）
#   POST /v1/traces/{id}/finalize        组装 + 异步触发蒸馏
#   GET  /v1/traces/{id}/status          轮询进度
# 详见知识库 browserbc-server-py.md。
